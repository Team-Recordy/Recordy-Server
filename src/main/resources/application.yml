spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:aws://dev.cxsgisoikksl.ap-northeast-2.rds.amazonaws.com:3306/dev
    username: recordy
    password: Recordy0615
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true